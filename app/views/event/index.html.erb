<div class="jumbotron jumbotron-fluid jumbotron-hero">
  <div class='container'>
    <div class="text-center">
    <br>
    <br>
    <br>
    <br>
    <h1>WELCOME ON EVENBRIT<h1>
      <h5>All events in your city</h5>
    <br>
    <br>
    <br> 
      <% if user_signed_in? %>
        <div class="text-center">
        
          <p>Voici ton email : <%= current_user.email %></p>
     
          <%= link_to "Edit email / password", edit_user_registration_path, class:'btn btn-outline-dark rounded-pill' %>

          <%= link_to "Sign out", destroy_user_session_path, method: :delete, class:'btn btn-outline-dark rounded-pill' %>
          
    ​    </div>
          <br>
          <br>
      <% else %>

          <%= link_to "Sign in", new_user_session_path, class:'btn btn-outline-light btn-lg rounded-pill'%>
          <%= link_to "Sign up", new_user_registration_path, class:'btn btn-outline-light btn-lg rounded-pill' %>
          <br>
          <br>
        <% end %>
        <div class='container'>
          <div class="card-columns">
          <% @events.each do |event| %>
            <div class="card">
              <div class="card-body">
                  <h4 class="card-title"><%= link_to event.title, event_path(event) %></h4>
                  <p class="card-text"><%= truncate(event.description, length: 100) %></p>
                  <% unless event[:admin_id] != event.admin_id %>
                  <p> <%= link_to "Edit", edit_event_path(event), class: "btn btn-outline-primary rounded-pill" %> 
                      <%= link_to "Delete", event_path(event), method: :delete, data: { confirm: "Are you sure you want to delete this event?"}, class:"btn btn-outline-danger rounded-pill" %>   
                  </p> 
                  <% end %>
              </div>
            </div>
          <% end %>
          </div>
        </div>
      </div>
    </div>